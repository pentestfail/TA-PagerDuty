[pagerduty_api_logentries://<name>]
api_token = API token with access to your Pagerduty instance.
api_limit = Max events per API request (100 max).
days_ago = Number of days in past to query for initial sync (days ago to now).

[pagerduty_api_incidents://<name>]
api_token = API token with access to your Pagerduty instance.
api_limit = Max events per API request (100 max).
days_ago = Number of days in past to query for initial sync (days ago to now).